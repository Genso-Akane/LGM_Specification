## Reduction of Cosmology. Part 5: Universe

### Causal Chain (Shortest Causally Sufficient Sequence)

`2. Event` → `9. Process` → `25. Universe`

### Mechanism and Computational Consistency

### Cause:

The existence of `9. Process` as a continuous causally connected chain of physical facts serves as the fundamental basis for the unfolding of reality. Outside processes, causal relations have no dynamic manifestation.

### Mechanism:





Minimal discrete units — `2. Events` — are integrated into chains of changes, whose density at every segment is computed through the canonical invariant of `11. Tempo of Processes`:

$$T_p = \frac{E_v}{P_r}$$





The unfolding and interference of these chains generate all intermediate causal nodes of the graph: from `3. Energy` and `6. Matter` to local `7. Systems` and their `8. System States`. Consistency of scales and changes between them is strictly determined by `16. Limiting Velocity of a Process` ($V_{lim} = \frac{Tr}{P_r}$).



The mathematical integral closure of all causal trajectories, processes, and acts of information generation throughout the entire domain of changes manifests as `25. Universe`. The complete unfolded engineering form of the `Universe` as a total process is defined by the final computational invariant:

$$U = \int\limits_{P_r} ( E_v \cdot C ) = \int\limits_{P_r} \left( E_v \cdot C \cdot \frac{E}{T_p} \cdot M \cdot Sys \cdot Inf \cdot Meas \cdot Sync \right)$$

### Consequence:

The concept of a global container is completely eliminated. `25. Universe` is neither a physical object, nor an astronomical body, nor a geometric spacetime, but the complete causal process possessing absolute connectivity:

No local `9. Process` or isolated `2. Event` can possess causal autonomy outside the integral `U`, since their parameters ($Ev, Tp, Inf$) are embedded within the common computational matrix.

Large-scale phenomena interpreted by classical physics as the “geometry of the world” are merely consequences of the distribution of event-density gradients within this unified metaprocess.

### Practical Conclusion (Engineering Ontology Fixation)

Any correct physical description or computation of a particular phenomenon is a local restriction (slice) of the unified `9. Process` within `25. Universe`.

### Analysis and Control:

Engineering intervention and regime reconfiguration are fundamentally possible and technically realizable exclusively at the level of local, distinct `7. Systems` through modification of their internal parameters and capacities.

### Global Limit:

Direct or indirect control of `25. Universe` as a whole is mathematically impossible, since the controlling system itself is an embedded variable ($Sys$) under the overall integral `U`.

### Prediction:

Computation of future states of the environment is implemented strictly through continuation (extrapolation) of the current causal steps of `9. Process` within the unified closed computational circuit of dynamics modification:

$$Dyn = \frac{E \cdot P_r^3}{E_v^2}$$

### Limitations:

All physical and informational simulation is rigidly constrained by the complete causal connectivity of the graph. Introduction of any concepts that violate this connectivity or cannot be reduced to the invariants of formulas `(0–25)` constitutes an ontologically inadmissible description error.
