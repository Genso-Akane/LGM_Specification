## Reduction of Cosmology. Part 1: Hidden Parameters

### Causal Chain (Shortest Causally Sufficient Sequence)

`3. Energy` → `11. Tempo of Processes` → `18. Gravitation` → `19. Gravitational Influence` → `20. Information` → `21. Measurement`

Mechanism and Computational Consistency

### Cause:

The existence of `3. Energy` as a quantitative characteristic of causality determines the capacity and ability of local circuits to participate in acts of physical interaction.

### Mechanism:





The realization of `4. Quanta of Action` within `9. Process` through `2. Events` establishes the fundamental density distribution of physical facts, forming `11. Tempo of Processes`. The canonical relation of tempo is rigidly fixed by the formula:

$$T_p = \frac{E_v}{P_r}$$





Dynamic inhomogeneity and spatial differences in event density generate `18. Gravitation`. The computational invariant determines the magnitude of gravitational stabilization of the regime as:

$$G_r = \frac{E}{T_p^2} = \frac{E \cdot P_r^2}{E_v^2}$$





The gradient of this field forms `19. Gravitational Influence` ($\nabla G_r$), which directly modifies accessible states and deforms the histories of changes of systems — their `14. Trajectories`.



The outcome structure of local interaction data manifests as `20. Information` (Inf = Ev), which, with the participation of the material framework of `7. System`, is fixed through `21. Measurement`:

$$Meas = Inf \cdot T_p = E_v \cdot T_p$$

### Consequence:

The observable phenomena of classical cosmology are completely reduced to anomalies and regimes of the unified computational circuit `(11–18–23–24)` without introducing external entities:

`“Dark Matter”` — a manifestation of `19. Gravitational Influence` ($G_i = \nabla G_r$) under conditions where the local tempo of processes Tp has changed, but a stable step of `6. Matter` has not been formed, resulting in the absence of direct generation of `20. Information`.

`“Dark Energy”` — a large-scale regime of change in the fundamental `11. Tempo of Processes` within the global `9. Process`, altering event density without localized fixation of parameters of `7. System`.

`“Cosmological Constant”` — a stationary, stable contribution to the background `11. Tempo of Processes` of the `Universe`, registered by instruments through a cascade of `21. Measurements`.

### Practical Conclusion (Engineering Control)

All available diagnostics of hidden parameters of cosmological objects are performed exclusively through the registration of deformations of `14. Trajectories` of neighboring systems.

Engineering analysis and modeling of the regime are performed using the final formula of `24. Energy-Conditioned Modification of Dynamics`:

$$Dyn = \frac{E \cdot P_r^3}{E_v^2}$$

Calculations are strictly limited by the limiting step of coordination of changes — `16. Limiting Velocity of a Process` ($V_{lim} = \frac{Tr}{P_r}$), excluding instantaneous action at a distance and any non-material geometric interpretations of `13. Space`.
