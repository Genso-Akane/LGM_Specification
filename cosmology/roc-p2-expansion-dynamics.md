## Reduction of Cosmology. Part 2: Expansion Dynamics

### Causal Chain (Shortest Causally Sufficient Sequence)

`3. Energy` → `11. Tempo of Processes` → `18. Gravitation` → `9. Process` → `20. Information` → `21. Measurement`

### Mechanism and Computational Consistency

### Cause:

The existence of `3. Energy` as the fundamental capacity of a system establishes the initial conditions and the original inhomogeneity of the distribution density of subsequent changes.

### Mechanism:





The generation of primary facts of reality `(2. Event)` unfolds into causally connected chains, forming `9. Process`. The density of these events within the process rigidly determines `11. Tempo of Processes`, calculated by the canonical invariant:

$$T_p = \frac{E_v}{P_r}$$





The dynamic inhomogeneity of this tempo forms `18. Gravitation`, fixed through the balance of capacity to the square of event density:

$$G_r = \frac{E}{T_p^2} = \frac{E \cdot P_r^2}{E_v^2}$$





Large-scale modification of `9. Process` itself occurs through a deterministic change in its internal interaction frequency. The change of a system’s history `(14. Trajectory)` functionally depends on `momentum` and `tempo`:

$$\Delta Tr \sim I \cdot P_r = \frac{E}{T_p} \cdot P_r$$





The outcome structure of each local event manifests as `20. Information` (Inf = Ev). A change in tempo Tp causally transforms the recorded structure, which is registered through `21. Measurement` on the material framework of `7. System`:

$$Meas = Inf \cdot T_p = E_v \cdot T_p$$

### Consequence:

Classical macroscopic concepts of cosmology are completely reduced to the dynamics of changes in event density and are computed without invoking the concept of “self-expanding empty space”:

`“Big Bang”` — the initial high-density regime of `9. Process`, characterized by extreme singular inhomogeneity and a maximum of `11. Tempo of Processes` ($T_p \to \max$) with a minimal step of causal acts.

`“Expansion of the Universe”` — a systemic shift in the characteristics of `9. Process` (an increase in the step between events), mathematically identical to a global change in `11. Tempo of Processes` on the scale of the metasystem.

`“Redshift”` — a regular distortion of `20. Information` (Inf), caused by the fact that between the act of event generation and the act of its fixation, the `11. Tempo of Processes` of transmission underwent causal drift.

`“Hubble Law”` — a stable functional dependence of the step of change of registered `20. Information` on the accumulated gradient of `11. Tempo of Processes` between remote canonical nodes, stably fixed through the apparatus of `21. Measurement`.

### Practical Conclusion (Engineering Control)

All observable cosmology of expansion is an engineering registration of changes in event density over `10. Time`.

Analysis of parameters and deconstruction of environmental `12. Entropy` are achieved strictly through `21. Measurement` of wave and corpuscular parameters of `20. Information` ($Meas = Inf \cdot T_p$).

Interpretation of registered shifts requires mandatory computation of local and large-scale coefficients of `11. Tempo of Processes` (Tp).

Prediction and modeling of the evolution of macrosystems are implemented through calculation of the behavior of `9. Process` within the unified computational circuit of dynamics modification:

$$Dyn = \frac{E \cdot P_r^3}{E_v^2}$$

Any coordinational and relativistic consistency of modeling sectors is rigidly limited by `16. Limiting Velocity of a Process` ($V_{lim} = \frac{Tr}{P_r}$), which excludes superluminal paradoxes and fixes causal connectivity within the graph.
