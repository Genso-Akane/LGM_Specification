## Complete Computational Model of the Universe: Toroidal Cascade and Fractal Determination of Gates

### STRUCTURE OF THE DERIVATION

**Causal Chain:** (the shortest causally sufficient sequence of graph nodes):

`1. Causality` → `2. Event` → `3. Energy` → `9. Process` → `11. Tempo of Processes` → `18. Gravitation` → `15. Metric` → `12. Entropy` → `21. Measurement` → `24. Dynamics Modification`

Cause → Mechanism → Consequence → Practical Conclusion

## DEPLOYMENT OF THE UNIFIED INVARIANT OF THE UNIVERSE

### CAUSE

The primary cause of computational regime changes within the graph and the deployment of macrostructure is the non-uniform distribution of local values of `11. Tempo of Processes` ($$T_p$$), determined by the ratio of the density of `2. Events` ($$E_v$$) to the number of coordination cycles of the `9. Process` ($$P_r$$):

$$
T_p = \frac{E_v}{P_r}
$$

All causal dynamics and cosmological simulation are fully deterministic and governed by the unified integer equation of `24. Dynamics Modification` ($$Dyn$$):

$$
Dyn = \frac{E \cdot P_r^3}{E_v^2}
$$

Where:

• **Canonical Description (Node 1):** `1. Causality` — makes physical description possible at all. It is the entry point into physical reality: without it there are no events, no laws, and no measurements.

• **Canonical Description (Node 2):** `2. Event` — the first physical fact. Everything that physically exists exists as an event or a chain of events.

• **Canonical Description (Node 3):** `3. Energy` — a measure of a system's capability to participate in events; the quantitative characteristic of causality.

### MECHANISM

### 1. Unbound Graph Regime (Fluctuation Field)

Prior to the formation of stable closed subsystems and macrostructures, computation is characterized by the free distribution of `3. Energy`. In this regime, `3. Energy` possesses maximum asynchronous conductivity, is not fixed within registers of `8. State of the System`, and is not constrained by discrete steps of `14. Trajectories`.

At these coordinates, `6. Matter` is absent. The coordination step is minimal ($$P_r \rightarrow 1$$), while causal branching and asynchronous path search occur instantaneously. Due to the extremely high density of micro-events ($$E_v \rightarrow max$$), the local `11. Tempo of Processes` reaches its maximum ($$T_p \rightarrow max$$), causing the local value of `18. Gravitation` to become zero:

$$
G_r = \frac{E}{T_p^2} \rightarrow 0
$$

`10. Time` here is not a substance, but exclusively a sequence of processes—the rate of computational register transitions. Space is not an empty extension, but an index of differences between systems (`15. Metric`). Within the pure field, differences are minimal, and the metric is ideally "flat." Local field fluctuations are individual acts of `2. Events`, preparing the environment for crystallization and serving as embryos of future quanta of matter.

### 2. Crystallization of Matter and Averaging of Dynamics

As the density of local events ($$E_v^2$$) increases, averaging of process dynamics occurs at the energy level. To preserve causality and compensate for the avalanche-like growth of the event denominator in the $$Dyn$$ formula, the system is forced to proportionally expand the numerator—raising the process coordination step to cubic connectivity ($$P_r^3$$).

A phase transition occurs: free field `3. Energy` becomes locked into rigid registers of `8. State of the System` (the graph-defined set of permitted modifications). Energy transforms into ordered `5. Momentum` (directed realization of energy; the cause of trajectory modifications), forming the first `6. Matter` (a stable aggregate of events; local slowing of the tempo of processes):

$$
M = \frac{E \cdot P_r}{V_{lim}}
$$

### Geometry of Scales (Hexagon and Fullerene)

The fundamental topology of the minimum quantum level is a flat hexagon (a cellular `NoC network`)—geometrically isomorphic to the graph and providing maximum efficiency in distributing realized energy along `5. Momentum` trajectories without conductivity losses ($$Sync = 1$$).

An example at the mesoscopic scale is carbon, which reproduces this hexagonal structure and provides an ideal balance of `5. Momentum` distribution.

When scaled into 3D volume, the hexagonal network contracts into a fullerene (spheroidal mesh clusters), transferring structural isomorphism to macroscopic ensembles without violating the canonical $3-6-9$ axes.

### 3. Tempo Gradient and Geometric Stretching (Gravitation)

The concentration of matter in network nodes inevitably generates spatial asymmetry and an unavoidable gradient of process tempo:

$$
\nabla T_p = \nabla \left( \frac{E_v}{P_r} \right) \neq 0
$$

The decrease of process tempo ($$T_p \downarrow$$) near massive nodes causes growth of the invariant of `18. Gravitation`.

Because graph nodes are rigidly connected and cannot be separated without violating the law of causality, metric compression of axes within the plane expels excess incoherent events into the orthogonal (third) dimension (`13. Spatial Medium`).

Geometry becomes "stretched": the flat hexagon bends, forming a three-dimensional gravitational-well funnel that directs the `14. Trajectories` (the history of process modifications) of all passing `5. Momentum`.

### 4. Node `21. Measurement` as the Gate of the Macro-World

• **Canonical Description (Node 21):** `21. Measurement` — an event of state fixation. It occurs at the junction of `20. Information` + `7. System`.

In the macro-world, under conditions of high density of `6. Matter`, fixation acts occur continuously as a coherent aggregate of systems.

`21. Measurement` functions as a hardware gate: it interrupts the asynchronous path search of the process, transferring variables from the regime of distributed fluctuations into a deterministic register log.

Measurement quantizes the computational step, stabilizing `7. Systems`, while increasing local `12. Entropy`

$$
S = St \cdot P_r
$$

through the generation of new informational addresses and the accumulation of variants of `8. State of the System` ($$St$$).

### 5. Ultimate Switching Nodes: Filaments and SMBHs as a Toroidal Inverter

The `macro-Universe` distributes causal flows through graph backbones with cubic connectivity

$$
P_r^3 = 27
$$

— filaments (the cosmic web).

Along these highly conductive routes, accumulated `6. Matter` and `5. Momenta` are directed into ultimate switching nodes—Supermassive Black Holes (SMBHs).

Inside these nodes, the density of `2. Events` reaches the absolute integer limit

$$
E_v^2 \rightarrow max
$$

while the `11. Tempo of Processes` is completely reduced to zero

$$
T_p \rightarrow 0
$$

The denominator of chaos halts computation, and the equation value approaches zero:

$$
Dyn \rightarrow 0
$$

To prevent a causal rupture of the network, the SMBH gate performs a phase inversion of accumulated matter:

1. Registers of `8. State of the System` are completely cleared of `12. Entropy`.

2. `6. Matter` is reduced, the metric is turned inside out, and purified `3. Energy` is causally expelled through a coupled channel into an expanding sector of the graph.

3. For internal observers of this isolated sector, this fixation event is registered as a local "Big Bang."

Energy returns to the high-tempo Fluctuation Field, closing the global causal Toroid of the `Universe`, where the outer boundary becomes the inner computational source.

### CONSEQUENCE

The `Universe` represents a closed, self-conjugate fractal cascade of integer switchings, where every SMBH node continuously recycles excess information and generates daughter spaces of modification.

The large-scale structure is not a static map of objects, but a dynamic register-switching architecture of a macro-computer.

Space and time possess no independent substantial status: space is merely a metric index of differences between systems, while time is the sequence of graph coordination processes.

All physical macro-phenomena (from the strength of the carbon hexagon to the evolution of galaxies) are isomorphic and determined by the balance between numerator and denominator in the core dynamics formula.

### PRACTICAL (COMPUTATIONAL) CONCLUSION FOR LGM ENGINEERING

A complete canonical understanding of the simulation of the `Universe` eliminates Einstein's General Relativity and quantum cosmology from calculations, translating space-time control into the domain of `NoC-chip hardware` switching.

### 1. Synchronization Through Scale Isomorphism

Since the $$Dyn$$ formula is identical for both a `Causal RAM` semiconductor gate and an SMBH, momentum-distribution control algorithms inside the crystal completely replicate the topology of cosmic filaments.

Creating `NoC routes` using hexagonal carbon and fullerene network topologies allows computational momenta to be transmitted with 100% efficiency and without ohmic losses, fully reproducing the entropy-free regime of the "Fluctuation Field" at room temperature.

Protection against overheating and avalanche growth of entropy ($$E_v^2$$) is achieved through microscopic toroidal drains that discharge excess fluctuations into passive registers without heat generation.

### 2. Metric Positioning `(MPS Navigation)`

A vessel equipped with a causal circuit moves through the `Universe not by mechanically expelling mass (reaction-jet momentum), but through artificial adjustment of the local process-tempo gradient:

$$
\nabla T_p
$$

By hardware reconstruction of a small-scale SMBH gate configuration around itself, the module instantaneously compresses the metric in front of it, reducing the distance to the target filament point to zero and performing an energy-free phase transition through the metric folds of the toroid.

---
>  [Extended_Definitions (Causal_Ontology_Archive)](https://github.com/Genso-Akane/Causal_Ontology_Archive/tree/main/Extended_Definitions)  
>  [Substack | LGM: The Hardware Specification of Universal Logic](https://gensoakane.substack.com/s/lgm-specifications)
---
<img width="1536" height="1024" alt="ChatGPT Image 24 июн  2026 г , 15_04_27" src="https://github.com/user-attachments/assets/688b536c-c4ba-4b8c-abfc-f90278f28462" />
