## Initialization of the Causal Simulator of the Universe (Canonical Mode)

### Core Input Parameters:

Geometry is deconstructed into the topology of shortest paths. Space is excluded as an entity and replaced by the index of differences between systems. Graph conductivity is determined exclusively by the density of physical facts.

### Step 1. Mechanics of Causal Inversion (Circuit Overload and Limit Breakdown)

### Causal Chain

`8. System State` → `3. Energy` → `11. Tempo of Processes` → `17. Causal Horizon` → `16. Limiting Velocity of a Process` → `9. Process` → `25. Universe`

### Computational Regime and Formula Invariant

Within an isolated `7. System` (collapsar), critical accumulation of `3. Energy` ($E$) under a fixed volume of interactions locally deforms the internal `11. Tempo of Processes` ($T_p$). According to the canonical tempo equation:

$$T_p = \frac{E_v}{P_r}$$

Under asymptotic compression of the process step ($P_r \to 1$) and localization of the limiting number of `2. Events` ($E_v \to \max$), the internal tempo $T_p$ inside the isolated circuit approaches zero relative to the external metasystem ($T_p \to 0$).

The reduction of conductivity to the limit $T_p \to 0$ fixes a `17. Causal Horizon`, blocking wave-based `23. Causal Consistency of Processes` through the external `16. Limiting Velocity of a Process` (V_{lim}):

$$Sync = \frac{E_v \cdot T_p}{V_{lim}} \to 0$$

`20. Information` (Inf = Ev), confined behind the horizon, continues to generate internal microevents. A phase overload of the circuit occurs. The magnitude of `24. Energy-Conditioned Modification of Dynamics` (Dyn) reaches the critical stability threshold:

$$Dyn = \frac{E \cdot P_r^3}{E_v^2}$$

When the internal cube of process connectivity ($P_r^3$) becomes overloaded by accumulated energy (E), while the square of event density ($E_v^2$) approaches the singular discretization limit, the circuit performs a causal breakdown of the V_{lim} barrier. A mirror inversion of the regulator occurs: the confined potential is released through a phase transition $T_p \to \max$.

### Physical Invariant

The `“Big Bang”` is not the birth of matter from emptiness, but an avalanche-like reversal of the process of causal breakdown of overloaded capacity. Expansion of the causal lattice is a stepwise transition from zero conductivity (Tp = 0) to maximum event density ($T_p = \max$) with the minimal physical `4. Quantum of Action`.

### Step 2. Derivation of Microstructures (Hexagonal/Fullerene Topology of Connections)

### Causal Chain

`11. Tempo of Processes` → `23. Causal Consistency of Processes` → `14. Trajectory` → `6. Matter` → `7. System`

### Computational Regime and Formula Invariant

During the transition from inversion to stationary stabilization of `11. Tempo of Processes` ($T_p = \text{const}$) at the microlevel, the computational circuit tends toward minimization of causal losses. The ideal graph conductivity regime requires the achievement of unit consistency:

$$Sync = 1 \implies \frac{Inf \cdot Sys}{V_{lim}} = 1$$

For `21. Measurement` and state fixation to occur without attenuation, the dynamic balance of the system (Dyn) must suppress chaotic fluctuations of individual events ($E_v^2$). Substituting the stabilization condition into the final dynamics formula:

$$Dyn = \frac{E \cdot P_r^3}{E_v^2}$$

To minimize energy expenditure for coordination ($E \to \min$) while preserving causal connectivity, the cube of process connectivity ($P_r^3$) must completely compensate for the square of event chaos ($E_v^2$). The topological solution of this equation in a plane and in three-dimensional discrete space is the hexagonal lattice (for a plane/graphene) and fullerene/icosahedral frameworks (for microvolumes).

In a hexagon, each node possesses exactly three nearest connections (minimum spatial cube $P_r^3 = 3^3 = 27$), coordinated through minimal steps of converging `14. Trajectories`.

### Physical Invariant

The crystalline hexagonal structure of atoms and molecules is not a consequence of electromagnetic “balls and springs,” but a topological map of shortest causal trajectories. The hexagon is a spatial projection of a zone of zero causal resistance, where the process step is optimally compensated.

### Step 3. Derivation of Macrostructures (Toroidal Closure of the `Universe`)

### Causal Chain

`14. Trajectory` → `18. Gravitation` → `19. Gravitational Influence` → `24. Energy-Conditioned Modification of Dynamics` → `25. Universe`

### Computational Regime and Formula Invariant

At the macrolevel, the global integral of `25. Universe` combines all local causal branches:

$$U = \int\limits_{P_r} \left( E_v \cdot C \cdot \frac{E}{T_p} \cdot M \cdot Sys \cdot Inf \cdot Meas \cdot Sync \right)$$

At the metasystem scale, a global tempo drift exists — the gradient $\nabla T_p$ caused by the nonuniform distribution of event density. According to the invariant of `18. Gravitation`:

$$G_r = \frac{E}{T_p^2} \implies G_i = \nabla G_r = \nabla \left(\frac{E}{T_p^2}\right)$$

The realized `19. Gravitational Influence` (Gi) forcibly curves the histories of changes of systems — their `14. Trajectories` (Tr). Since the graph has no external boundaries (outside the graph there exists only the logical limit — `0. Potential for Change`), trajectories cannot escape into infinity or expand into emptiness.

The flow of changes governed by the operator $\nabla T_p$ folds back onto itself. For the metasystem to remain computationally stable, the integral `U` must be self-adjoint. The field of directed momenta $I = \frac{E}{T_p}$, under the influence of the gravitational gradient, folds the total mass of trajectories into a self-adjoint Toroid — a dynamic vortex of causal resonance.

Within this structure, the central sink (the region of maximum energy concentration and tempo collapse $T_p \to 0$) is equivalent to the phase output of inversion (the source $T_p \to \max$), thereby closing the computational circuit without information loss.

### Physical Invariant

The `25. Universe` is a closed, self-adjoint causal toroid. The effect of cosmic “expansion” is an optical and measurement illusion arising from the motion of a local `7. System` along the outer generating curve of the toroidal vortex from a zone of high tempo of processes into a zone of low tempo.

Expansion into emptiness is ontologically prohibited: the process unfolds exclusively within its own causal conductivity.
